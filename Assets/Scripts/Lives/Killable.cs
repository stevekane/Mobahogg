using UnityEngine;

/*
System responsible for killing an entity when its health falls below
some threshold or it is explicitly killed by other means.
*/
public class Killable : MonoBehaviour {
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
